# Design Doc: 約定通知の通知先エンドポイント複数登録対応

| 項目 | 内容 |
|---|---|
| Status | **Approved** |
| Author | 山田 |
| Reviewers | 佐藤（承認必須・アーキテクト）、鈴木（承認必須・DB）、田中（任意コメント・運用） |
| Approved At | 2026-07-10 14:30（非同期承認・コメントスレッド #42 参照） |
| Tier | 2（DBスキーマ変更を含むため） |
| 関連 | JIRA: NOTIF-215 / 先行doc: 「約定通知配信基盤」design doc / Confluence仕様書: 実装完了後にリンク |

> ※これはテンプレート定着用のサンプルです。レビュー完了後の状態（Decision Log記入済み）を再現しています。数値・名前は架空。

---

## 1. Context / Background

現行の約定通知基盤は、クライアント企業1社につき通知先エンドポイントを1つしか登録できない。
クライアントA社【仮置き】から「本番系と検証系の2系統に同時配信してほしい」との要望があり、営業経由で対応依頼が来ている（NOTIF-215）。
また運用チームからも、クライアント側のエンドポイント移行時に「旧新並行配信ができず切替が一発勝負になる」問題が以前から挙がっている。

## 2. Goals

- クライアント1社につき通知先エンドポイントを最大5件【仮置き】登録可能にする
- エンドポイントごとに有効/無効を切り替え可能にする（移行時の並行配信・切り離しに使う）
- 既存の1エンドポイント登録済みクライアントに影響を与えない（データ移行含めゼロダウンタイム）

## 3. Non-Goals

- エンドポイントごとの配信内容のフィルタリング（口座別・イベント種別の出し分け）はやらない。要望が実在しないため。スキーマ上の拡張余地も特に設けない（必要になったらALTERで足せる範囲）
- クライアント自身によるエンドポイント設定のセルフサービス化はやらない（先行docのNon-Goals踏襲。設定変更は当社運用チーム経由のまま）
- 配信リトライ・DLQの仕様は変更しない（エンドポイント単位で独立動作させるのみ）

## 4. 提案する設計

### 4.1 全体構成

変更箇所は通知ディスパッチャの購読設定解決部分と、設定を保持するテーブルのみ。

```
[通知ディスパッチャ]
   │ 購読設定の解決: client_id → endpoint 1件
   │ を client_id → 有効な endpoint N件 に変更
   ▼
[配信ワーカー]  ※エンドポイントごとに独立した配信ジョブを生成
```

### 4.2 設計上の判断点

**判断1: 配信ジョブはエンドポイント単位で生成する（クライアント単位でN送信ではなく）**
理由: 片方のエンドポイントの障害・リトライがもう片方の配信に影響しないようにするため。移行用途（旧新並行）では新側が不安定なことが多く、独立性が必須。
引き換えに: 配信状態レコードがエンドポイント数分に増える。A社の場合2倍。全クライアントが5件登録した最悪ケースでも書き込み量は現行の5倍で、負荷試験済みの余裕（10倍）の範囲内。

**判断2: sequence_number はクライアント×口座単位のまま変えない（エンドポイント単位にしない）**
理由: シーケンスは「業務イベントの順序」を表すもので、配信経路の都合を持ち込むべきでない。同一イベントは全エンドポイントに同一 sequence_number で届く。クライアント側の欠番検知ロジックにも変更不要。
引き換えに: エンドポイント単位の「どこまで届いたか」はシーケンスでは追えないが、それは配信状態テーブルの責務なので問題ない。

## 5. 不可逆要素の定義

### 5.1 DBスキーマ変更

```sql
-- 判断: client_config テーブルの endpoint_url カラムを廃止し、別テーブルに正規化
-- 理由: カラム追加（endpoint_url_2, _3...）は上限変更のたびにALTERが必要になり、
--       有効/無効フラグも持てない。移行コストを払ってでも正規化する
CREATE TABLE client_endpoint (
  endpoint_id   CHAR(26)     NOT NULL,           -- ULID
  client_id     VARCHAR(20)  NOT NULL,
  endpoint_url  VARCHAR(500) NOT NULL,
  hmac_secret_ref VARCHAR(100) NOT NULL,         -- 判断: シークレットはエンドポイント単位で個別発行
                                                 -- （旧新移行時に新側だけローテーション可能にするため）
                                                 -- 値そのものは持たずVaultの参照キーのみ【仮置き: secrets管理の社内標準】
  is_active     BOOLEAN      NOT NULL DEFAULT TRUE,
  created_at    TIMESTAMP    NOT NULL,
  updated_at    TIMESTAMP    NOT NULL,
  PRIMARY KEY (endpoint_id),
  CONSTRAINT uq_client_url UNIQUE (client_id, endpoint_url)
);
CREATE INDEX idx_client_active ON client_endpoint (client_id, is_active);
-- 判断: 上限5件はDB制約ではなくアプリケーション側でチェック
-- 理由: 上限は業務ルールであり変わりうる。CHECK制約での表現も煩雑
```

- 既存データ移行: `client_config.endpoint_url` から `client_endpoint` へ INSERT するマイグレーションを同PRに含める。移行後も旧カラムは**1リリース分残置**し（読み取り側の切替完了後）、次のリリースで DROP する（expand-contract）
- ロールバック可否: 旧カラム残置期間中は可（読み取りを旧カラムに戻すだけ）。DROP後は不可
- `notification_delivery` テーブルには `endpoint_id` カラムを追加（NULL許容で追加→バックフィル不要、新規レコードのみ記入)

### 5.2 対外インターフェース変更

なし（クライアント向けのペイロード・署名方式・再取得APIに変更なし。同一イベントが複数届く形になるが、各エンドポイントから見た仕様は現行と同一）

### 5.3 イベント/メッセージスキーマ変更

なし

## 6. Alternatives Considered

**案: client_config にカラム追加（endpoint_url_2）で済ませる**
利点: マイグレーションが軽い。A社の要望（2系統）だけなら足りる。
欠点: 有効/無効の切替が持てず、運用チームの並行配信ニーズに応えられない。3件目の要望が来たら再度ALTER。
不採用理由: 移行コストの差が小さい割に、半年以内に作り直しになる可能性が高い。

正規化 vs JSON型カラム（endpoint設定をJSONで1カラムに持つ）も一瞬検討したが、UNIQUE制約・インデックスが素直に効かないため実質的な選択肢にならず。

## 7. セキュリティ・コンプライアンス考慮

- HMACシークレットをエンドポイント単位に分離することで、シークレット漏洩時の影響範囲と失効手順が現行より改善する（現行はクライアント単位で全配信停止が必要だった）
- 通知先URLの追加は要保護情報の送信先追加にあたるため、**エンドポイント登録時の承認フローは現行の運用手順（クライアント書面依頼→運用チーム2名確認）を変えずに適用**する。本変更で自動化はしない
- その他の観点: 該当なし

## 8. 運用・移行

- リリースは2段階（expand-contract）: ①テーブル追加＋二重書き込み＋読み取り切替 → ②旧カラムDROP
- A社への並行配信開始はリリース①完了後、運用チームの通常手順で実施

## 9. Open Questions

1. ~~上限5件の根拠は？~~ → **解決**: 根拠なし（仮置き）だったが、レビューで「上限は運用ガード目的なので当面5で確定、変更はアプリ設定値」と決定（Decision Log #2）
2. ~~is_active=false のエンドポイントへの配信中ジョブはどう扱う？~~ → **解決**: 無効化時点でPENDINGのジョブはキャンセルする。「無効化＝即時停止」が運用の期待と一致（Decision Log #3）
3. 旧カラムDROP（リリース②）の実施時期の判断基準 → **未解決のまま実装着手**: リリース①の安定稼働2週間後を目安とするが、確定判断は運用チームと別途。実装をブロックしない

## 10. レビュー決定記録（Decision Log)

| # | 指摘の要旨 | 提起者 | 決着 | 反映先 |
|---|---|---|---|---|
| 1 | 同一イベントが複数エンドポイントに届くと、クライアント側で「重複」に見えないか。event_id が同一なので冪等処理で片方捨てられる懸念 | 佐藤 | 意図した動作と整理。2系統受信するクライアントは系統ごとに独立して処理する前提であり、系統間の重複排除はむしろ有害。クライアント実装ガイドに「エンドポイントごとに独立した受信系として扱うこと」を追記する対応とし、設計は変更しない | §4.2 判断2に理由追記、実装ガイド改訂タスク起票（NOTIF-221） |
| 2 | 上限5件の根拠が仮置きのまま | 鈴木 | 運用ガード目的の値であり厳密な根拠は不要と整理。5で確定、アプリ設定値として保持 | §2、§9-1 |
| 3 | is_active切替と配信中ジョブの競合が未定義 | 田中 | 無効化時点のPENDINGジョブはキャンセル。運用の「止めたい」の期待に合わせる | §9-2、§4.2に影響なし（ワーカー実装の受け入れ条件としてJIRAに記載） |
| 4 | hmac_secret_ref の命名がVault前提すぎる。secrets基盤が変わったら？ | 鈴木 | 参照キー文字列という抽象で十分汎用と判断し、変更なし。「Vault」への言及はコメントから削除 | §5.1 コメント修正 |
