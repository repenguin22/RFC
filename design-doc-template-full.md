# Design Doc テンプレート（全部盛り版・削り込み用）

> **この版の読み方**: 各セクション冒頭の `[判定]` が削り込み用の私見です。
> - **必須** — これが無いとdesign docとして機能しない
> - **必須(条件付き)** — 該当する変更内容のときだけ必須
> - **推奨** — 迷ったら残す。ただし分量上限つき
> - **削除候補** — 最初の運用からは外してよい。必要になったら戻す
>
> 判定理由も併記したので、チームで議論して確定後、この注記ごと消して運用版にしてください。

## よくある質問（導入時の想定問答）

**Q. JIRAチケットと重複しないか？**
A. 扱う情報の種類が違う。JIRAが持つのは**要求と進捗**（何が欲しいか、いつまでに、今どの状態か）。design docが持つのは**技術的判断とその理由**（なぜこの方式か、何を検討して捨てたか、どんな制約を受け入れたか）。重複するのはContextの数行だけで、そこはJIRAへのリンクで済ませる。逆にdocには納期・担当・進捗を書かない。

**Q. 設計議論はJIRAのコメント欄でよくないか？**
A. 2つの理由で不可。(1) コメント欄は時系列に流れるだけで「最新の合意が何か」を保持する構造がない。docは本文＝最新の合意、コメントスレッド＝議論の過程、Decision Log＝論点の索引、という分担で常に結論が読める状態を保つ。(2) 寿命が違う。チケットはクローズ後に参照されないが、設計判断は数年後に「なぜこうなっている？」と参照される。監査証跡としても、クローズ済みチケットのコメント欄より承認記録付きdocの方が強い。

**Q. 実装が終わったらこのdocは更新するのか？**
A. **更新しない**。docは「その時点の意思決定の記録」であり、最新仕様のリファレンスではない（それはコードとConfluence仕様書の役割）。実装中に設計が根本的に変わった場合のみ、Supersededにして後続docを起こす。

---

## 0. メタ情報ヘッダ

**[判定: 必須]** — 承認の証跡になるのはここ。特に Status と Reviewers/承認者は監査観点で外せない。

| 項目 | 内容 |
|---|---|
| Status | Draft / In Review / **Approved** / Superseded |
| Author | |
| Reviewers | ※承認必須者と任意コメント者を区別する |
| Approved At | ※Approvedに変更した日時。非同期承認の証跡 |
| Tier | 2 / 3 |
| 関連 | 親チケット、関連design doc、Confluence仕様書（実装後にリンク） |

> 運用メモ: Status遷移のルール（誰がApprovedにできるか）はテンプレート外の運用ルールで定義。「Superseded」は後続docに置き換えられたときに使う。陳腐化ではなく世代交代として扱うため。

---

## 1. Context / Background

**[判定: 必須。ただし上限5行]** — 「なぜやるか」が無いとレビュアーは妥当性を判断できない。一方でここはAI生成の水増しが最も出やすいセクション（業界動向とか一般論を書き始める）。**上限を行数で縛るのが実効的**。

書くこと: 現状の何が問題か。この変更のトリガー（要望、障害、規制対応）。
書かないこと: 業界動向、技術の一般的な説明、既存システムの網羅的な紹介。

---

## 2. Goals

**[判定: 必須。箇条書き3〜7個]** — レビューの判断基準そのもの。「この設計はGoalsを満たすか」がレビューの主質問になる。

**書き方**: 良いGoalかどうかは**「設計案を却下する理由になれるか」**で判定する。「〜機能を実装する」は何を作っても満たせるため採点基準にならない（それはタスクであってGoalではない）。次の3つの型のどれかに当てはめる:

- **制約型**: 「〜しても壊れない」「〜に影響しない」（例: 既存クライアントに影響を与えない・ゼロダウンタイム）
- **能力型**: 「（利用者）が〜できるようになる」（例: 運用チームが旧新並行配信でエンドポイント移行を実施できる）
- **水準型**: 数値・SLOで検証可能な形（例: 約定確定から5秒以内・p99）

**禁止則**: 「実装する」「対応する」「開発する」「作成する」を述語にしたGoalは書き直し。これらの動詞が出たらタスクの言い換えになっているサイン（AI生成時の指示にもこの禁止則をそのまま含める）。

---

## 3. Non-Goals

**[判定: 必須]** — 削りたくなるが削ってはいけない筆頭。理由は3つ: (1) レビューで「〇〇は考慮した？」系の発散を先回りして潰す、(2) 実装者（人間・AI問わず）の過剰実装を防ぐ、(3) 後から「なぜやらなかった」と言われたときの意思決定証跡。うちの文脈だと(3)が監査的に効く。

書き方のコツ: 「やらない」だけでなく「やらない理由」または「どこで扱うか」を一言添える（例:「訂正・取消通知は対象外。イベント種別の拡張余地のみスキーマに残す」）。

---

## 4. 提案する設計

**[判定: 必須。ただし中身の粒度に注意]** — 方式レベルで書く。コンポーネント構成、データフロー、責務分担。処理の逐次的な説明を書き始めたら粒度が細かすぎるサイン。

### 4.1 全体構成図

**[判定: 推奨]** — テキスト図（ASCII/Mermaid）で十分。作図ツールでの清書は禁止レベルで不要。図の美しさに時間を使う文化が始まると本末転倒。

### 4.2 設計上の判断点

**[判定: 必須]** — このdocの魂その1。「AとBで迷ってAにした。理由は〜」を判断ごとに書く。各判断には**「引き換えに失うもの・受け入れるリスク」を1行添える**（独立したTrade-offs/Risksセクションは設けない。判断とトレードオフを離すと重複と抜けの両方が起きるため）。レビュアーに見てほしい判断には明示的に「ここは異論歓迎」と書く運用を推奨。

---

## 5. 不可逆要素の定義（one-way door セクション）

**[判定: 必須(条件付き) — Tier 2の該当要素があるときは絶対必須。無ければセクションごと削除]** — うちのプロセスの存在理由。ここの合意が「PRで根本からひっくり返る」のを防ぐ本丸。

### 5.1 DBスキーマ変更

**[判定: 該当時必須]** — 書式を固定する: **マイグレーションファイル相当のDDLを貼る＋判断コメントを付ける**。自由記述の表形式（論理名/物理名/型/説明の表）は禁止。理由: DDLなら実装とコピペで一致させられるが、表は転記時に乖離する。AIに書かせても水増しされない（DDLは構文が縛る）。

```sql
-- 判断: 通知状態は約定テーブルに持たせず別テーブルに分離
-- 理由: 約定テーブルは注文系の管理下。通知都合のUPDATEを混ぜない
CREATE TABLE notification_delivery (
  event_id        CHAR(26)    NOT NULL,  -- ULID
  client_id       VARCHAR(20) NOT NULL,
  sequence_number BIGINT      NOT NULL,  -- 判断: 採番単位は client×account（詳細 §4.2）
  status          VARCHAR(12) NOT NULL,  -- PENDING/DELIVERED/DLQ
  ...
  PRIMARY KEY (event_id)
);
-- 判断: (client_id, sequence_number) にUNIQUE制約。欠番検知APIの走査用
```

必ず書くこと: 既存テーブルへの影響（ALTER の有無）、データ移行の要否、ロールバック可否。
インデックス: 性能判断が絡むもののみ記載。自明なものは実装PRに委ねる。

### 5.2 対外インターフェース変更

**[判定: 該当時必須]** — 書式: リクエスト/レスポンスの実例（実データ形式）＋判断コメント。全フィールドの定義表は書かない。正はOpenAPI/Protoファイルとし、ここには「判断が埋まっているフィールド」だけコメント付きで載せる（例: 「価格は文字列。IEEE754回避」）。

### 5.3 イベント/メッセージスキーマ変更

**[判定: 該当時必須]** — 書式は5.2と同じ。加えて互換性の扱い（後方互換を壊すか、バージョニング方針）を必ず一言。

---

## 6. Alternatives Considered（検討した代替案）

**[判定: 必須。分量はTierで変える]** — このdocの魂その2。ただし全部盛りにすると「アリバイ用の藁人形案」を並べる悪習が生まれる。**本当に迷った案だけ**を書く。迷わなかったなら「実質的な代替案なし（〇〇が制約で一意に決まる）」と1行書けばよい。それも立派な記録。

**Tier別の深さ**:
- **Tier 2**: 1案あたり上限5行。日常運用でここを重くしない
- **Tier 3**: 上限を外す。主要な代替案は**それを本気で推す人が読んで「検討が浅い」と言えない深さ**まで書く（運用コストの内訳、必要な監視・体制、移行手順の具体性まで踏み込む。親会社RFC水準）。決定を覆すコストが大きいほど「なぜ他の道を選ばなかったか」の記録価値が上がるため。代替案の一般的な長所短所・運用要件の洗い出しはAIに下書きさせてよい（採否の判断と理由は著者が書く）

書式: 案の概要 / 利点 / 欠点 / 不採用理由。「不採用だが将来の拡張点として残す」のような中間判断はむしろ積極的に書く。

---

## 7. セキュリティ・コンプライアンス考慮

**[判定: 必須(条件付き)]** — 一般企業のテンプレートなら「推奨」だが、**うちは金融なので該当観点があれば必須、無ければ「該当なし」と明記**（書かないのと「無いと判断した」は監査上別物）。

チェック観点（該当するものだけ書く）: 要保護情報の流れの変化 / 認証・認可の変更 / ログに残せない情報 / 保全要件（期間・改ざん防止） / 対外接続の追加。

---

## 8. 運用・移行

**[判定: 推奨。上限10行]** — リリース手順の詳細は書かない（それは実装後の話）。書くのは**方式判断に影響する運用制約**のみ: 並走期間の有無、切り戻し戦略、監視で何を見るか。ここが方式選定に影響しないなら削除可。

---

## 9. Open Questions

**[判定: 必須]** — 削りたくなるが残すべき。「まだ決めてない点」を正直に書く文化がないと、docが「完璧を装う文書」になり作成コストが跳ね上がる。**未決事項があってもレビューに出してよい**というシグナルとして、空でもセクション自体は残す（「なし」と書く）。

レビュー後の運用: 解決した項目は消さず「→ 〇〇に決定（コメントスレッド参照）」と追記。ここが議論の索引になる。

---

## 10. レビュー決定記録（Decision Log）

**[判定: 必須。ただしレビュー開始後に書く（Draft時点では空）]** — レビューで発生した**主要な**論点と決着を残す場所。「議論の全文はコメントスレッド、結論は本文、論点の決着一覧はここ」という3点分担の3つ目。監査観点では「指摘されたが設計を変えないと判断した」記録が特に重要（対応しなかったこと自体が意思決定であるため）。

書式:

| # | 指摘の要旨 | 提起者 | 決着 | 反映先 |
|---|---|---|---|---|
| 1 | 部分約定の順序保証が必要では | △△ | 業務部門に確認、注文単位の順序要件なしと回答を得た。シーケンス番号による欠番検知のみとする | §4.3（変更なし）、スレッド参照 |
| 2 | sequence_numberの採番単位が細かすぎないか | □□ | 口座単位を維持。グローバル採番は配信系のボトルネック化リスクが上回ると判断 | §5.2 判断コメント追記 |

運用ルール:
- **全コメントを転記しない**。誤字指摘や単純なQ&Aは対象外。「設計判断に関わる論点」だけを著者が選んで記録する（目安: 1docあたり3〜10件。20件を超えたら転記しすぎ）
- 「反映先」列で本文修正箇所またはコメントスレッドへ誘導し、詳細はそちらに委ねる
- **本文を修正した場合もここに1行残す**。本文は常に最新の合意を示すため修正の痕跡が消える。その痕跡の索引がこの表
- Approved後にこの表へ追記が発生する場合（実装中の設計変更）は、軽微なら追記＋関係者への再確認、根本的ならdocをSupersededにして後続docを起こす

---
---

# 削り込みの推奨まとめ

**確定構成（チーム決定 2026-07-16）**:
0. メタ情報 / 1. Context(5行) / 2. Goals / 3. Non-Goals / 4. 提案する設計＋判断点(トレードオフ込み) / 5. 不可逆要素(該当時) / 6. Alternatives(迷った案のみ) / 7. セキュリティ(該当なし明記可) / 8. 運用・移行(任意) / 9. Open Questions / 10. レビュー決定記録(レビュー開始後に記入)

**削除決定済み（復活させない）**: Trade-offs/Risks独立セクション(§4.2に吸収) / 用語集 / 参考資料

**分量の目安**: Tier 2の標準案件で全体2〜4ページ。5ページを超えたら「詳細を書きすぎ」か「案件がTier 3相当」のどちらかを疑う。

**AI生成する場合の指示に含めるもの**: このテンプレートの構成に従うこと / 各セクションの上限行数 / **「テンプレートに無いセクションの追加は禁止。特に用語集・参考資料・リスク一覧・まとめ・今後の展望は生成しない」**（AIが自発的に生やす頻出セクションのため、名指しの禁止が必要）/ 一般論・背景説明の禁止 / Goalsに「実装する・対応する・開発する・作成する」を述語にした項目を書かない（§2の禁止則）。
