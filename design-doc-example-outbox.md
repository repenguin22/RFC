# Design Doc: 約定イベント発行の信頼性改善 — 二重書き込みの解消（Transactional Outbox方式への移行）

| 項目 | 内容 |
|---|---|
| Status | **Approved** |
| Author | 山田（通知基盤チーム） |
| Reviewers | 佐藤（承認必須・アーキテクト）、高橋（承認必須・注文管理チームリード）、鈴木（承認必須・DB）、田中（任意・運用）、伊藤（任意・リスク管理部システム担当） |
| Approved At | 2026-07-14 11:00（Decision Log #3 のみ対面レビュー実施、議事はスレッド #58 に転記） |
| Tier | **3**（注文管理・通知基盤のクロスチーム。約定エンジンのトランザクション構造に触れる） |
| 関連 | JIRA: NOTIF-230 / 障害報告: INC-2026-0412 / 先行doc: 「約定通知配信基盤」 / 親会社RFCプロセス準拠 |

> ※テンプレート定着用のサンプル（重量級・Tier 3の例）。レビュー完了後の状態を再現。数値・名前・障害は架空。

---

## 1. Context / Background

約定エンジンは現在、約定確定時に「①業務DBへのコミット」と「②Kafkaへのイベント発行」を**別々に実行**している（二重書き込み）。4月の障害（INC-2026-0412【仮置き】）では、DBコミット成功後・Kafka発行前にプロセスが停止し、約定23件の通知イベントが欠損、SFTP連携との突合で検知されるまで約40分を要した。
現行の欠損検知は日次突合バッチのみで、「at-least-once配信」を謳う通知基盤の保証が発行元で破れている状態。再発防止策として恒久対応が求められている（リスク管理部指摘事項 R-2026-08【仮置き】）。

## 2. Goals

- DBコミットとイベント発行の間で欠損が**構造的に発生しない**こと（プロセス停止・NW断のタイミングに依存しない）
- 約定処理のレイテンシ劣化を p99 で +10ms 以内に抑えること【仮置き: 現行 p99 45ms】
- 約定エンジンの業務ロジック（注文管理チームの管轄コード）への変更を最小にすること
- 移行期間中も通知の欠損・重複増加を発生させないこと（段階移行可能であること）

## 3. Non-Goals

- イベント発行の exactly-once 化はしない。アウトボックス方式はat-least-once（再送で重複しうる）であり、下流の冪等処理（event_id）は既存設計のまま前提とする
- 約定以外のイベント（注文受付・訂正・取消）の発行経路は今回触らない。ただし将来同方式に載せられる汎用性は判断基準に含める（§6参照）
- Kafka自体の可用性向上（クラスタ構成変更）はスコープ外。本docは「DBとKafkaの間」だけを扱う
- 日次突合バッチの廃止はしない。防御層として残す（欠損ゼロの実績が四半期継続した時点で縮退を別途検討）

## 4. 提案する設計

### 4.1 全体構成

Transactional Outbox パターンを採用する。約定エンジンは業務データと同一トランザクションでアウトボックステーブルにイベントを書き込み、独立したリレープロセスがそれをKafkaへ発行する。

```
[約定エンジン]
   │ 単一トランザクション:
   │   ① 約定テーブル INSERT
   │   ② outbox テーブル INSERT   ← 同一TXなので原子性が保証される
   ▼
[業務DB]
   ▲ ポーリング（100ms間隔【仮置き】）
   │
[Outboxリレー] ──発行──▶ [Kafka] ──▶ [通知ディスパッチャ（既存・変更なし）]
   │ 発行成功後に published_at を記録
```

- 欠損が構造的に消える理由: イベントの存在がDBトランザクションで保証され、リレーの停止は「発行の遅延」にしかならない（再起動後に未発行分から再開）
- リレーの再送により重複発行はありうるが、下流は event_id 冪等で既に吸収できる（Non-Goals参照）

### 4.2 設計上の判断点

**判断1: CDC（Change Data Capture / Debezium等）ではなくポーリング型リレーで始める**
理由: CDCはDBのレプリケーションログ購読の運用（コネクタ管理、スキーマ変更時の追随）が新たに乗る。当社にCDC運用実績がなく、障害対応の一次切り分けができる人員がいない。ポーリングは枯れた技術で、100ms間隔なら遅延要件（通知SLO 5秒）に対し十分。
引き換えに: DBへの定常ポーリング負荷と、CDCより高い発行遅延（最大ポーリング間隔分）を受け入れる。**リレーのI/Fはポーリング/CDCどちらでも成立する形に切っておき、将来の差し替えを妨げない**（ここは異論歓迎 → Decision Log #3 で対面議論、結論維持）。

**判断2: outboxテーブルは約定エンジンのDB（注文管理チーム管轄）に置くが、テーブルのオーナーは通知基盤チームとする**
理由: 同一TXの要件上、物理配置は業務DB内しかありえない。一方でoutboxのスキーマ・保持期間・リレーの運用は通知都合なので、変更管理の責任は通知基盤チームが持つ。管轄をまたぐため本docをTier 3とし、注文管理チームリードを承認必須者に含めた。
引き換えに: 注文管理チームのDBに他チーム管轄オブジェクトが同居する管理の複雑さ。テーブル命名プレフィクス（`notif_`）と、変更時の相互レビュー必須ルールで緩和する。

**判断3: 約定エンジン側の変更は「outbox INSERT関数の呼び出し1箇所」に限定する**
理由: Goals 3（業務ロジック変更最小）。イベントのシリアライズ・スキーマ管理は通知基盤チーム提供のライブラリに閉じ込め、約定エンジンは業務オブジェクトを渡すだけにする。
引き換えに: ライブラリのバージョン管理という結合が生まれる。スキーマ変更時は後方互換必須（§5.3）。

**判断4: 順序保証はパーティションキー（client_id×account_id）単位で維持する**
理由: リレーは outbox の採番順に読み、同一キーは同一Kafkaパーティションへ発行する。先行docの sequence_number 設計（口座単位の単調増加）と整合し、下流の欠番検知に影響を与えない。
引き換えに: リレーの並列度はパーティションキー単位に制約される。スループット試算は §8 の負荷試験で確認（試算上は現行ピーク500約定/秒【仮置き】の6倍まで余裕）。

## 5. 不可逆要素の定義

### 5.1 DBスキーマ変更

```sql
-- 判断: outboxは約定専用にせず event_type を持つ汎用構造にする
-- 理由: Non-Goalsの通り他イベントは今回載せないが、テーブル構造だけは
--       将来の注文受付・訂正イベントを追加コストゼロで受けられる形にする
--       （テーブル追加はTier 2案件になるが、行の種類追加なら実装のみで済む）
CREATE TABLE notif_outbox (
  outbox_id     BIGINT       NOT NULL,  -- 判断: 採番はDBシーケンス。リレーの読み出し順の正
                                        -- ULIDでなくBIGINTなのは「間隙のない順序走査」が必要なため
  event_id      CHAR(26)     NOT NULL,  -- ULID。下流の冪等キー（既存設計と同一）
  event_type    VARCHAR(30)  NOT NULL,  -- 'EXECUTION' のみ（当面）
  partition_key VARCHAR(50)  NOT NULL,  -- client_id:account_id
  payload       JSONB        NOT NULL,  -- 判断: ペイロード全体を保持（参照でなく実体）
                                        -- 理由: リレーが業務テーブルをJOINしない＝業務スキーマ変更の影響を受けない
  created_at    TIMESTAMP    NOT NULL,
  published_at  TIMESTAMP,              -- NULL=未発行。リレーの再開点
  PRIMARY KEY (outbox_id)
);
CREATE INDEX idx_unpublished ON notif_outbox (outbox_id) WHERE published_at IS NULL;
-- 部分インデックス: リレーの読み出しは未発行行のみ走査。発行済み行の肥大化の影響を受けない

-- 保持期間: 発行済み行は7日で物理削除（日次バッチ）。
-- 判断: 監査保全はKafka側とWORMログが担うため、outboxは「配送待ち行列」として短期保持でよい
--       → Decision Log #4: リスク管理部確認済み
```

- ロールバック可否: リレー停止＋約定エンジンの旧経路（直接発行）へのフラグ切替で可（§8の移行設計参照）。**テーブルDROPは移行完全完了後の別Tier 2案件とする**
- payload に約定情報（要保護情報）が平文で入る。アクセス制御は約定テーブルと同一のロール設計を適用（§7）

### 5.2 対外インターフェース変更

なし（クライアント向けIFは一切変更なし。イベントの「作られ方」だけが変わる）

### 5.3 イベント/メッセージスキーマ変更

Kafkaトピックのメッセージスキーマは変更なし。ただし**発行者が約定エンジンからリレーに変わる**ため、メッセージヘッダの producer 識別子が変わる（監視ダッシュボードのクエリ修正が必要 → 運用引き継ぎ事項）。
イベントペイロードのシリアライズは通知基盤チーム提供ライブラリに移管。以後のスキーマ変更は後方互換必須（フィールド削除・型変更の禁止、追加のみ許可）とし、互換性チェックをライブラリのCIに組み込む。

## 6. Alternatives Considered

**案A: 二重書き込みの改善（リトライ強化＋発行失敗時のローカル退避）**
利点: 変更が約定エンジン内で完結。テーブル追加なし。
欠点: 「コミット後・発行前のプロセス停止」という根本原因に対してはローカル退避ファイルも同時に失われるケース（ディスク障害）が残る。構造的解決にならない。
不採用理由: Goals 1（構造的に発生しない）を満たさない。リスク管理部指摘への回答としても弱い。

**案B: CDC（Debezium）で約定テーブルを直接購読**
利点: outboxテーブル自体が不要。約定エンジンの変更ゼロ。発行遅延も最小。
欠点: 業務テーブルのスキーマがそのままイベントスキーマに漏れる（業務DDL変更が下流を壊す結合）。CDC基盤の運用ノウハウが社内にない。DBバージョンアップ時の検証項目が増える。
不採用理由: 技術的には最有力対抗。運用成熟度の問題で今回は見送り、**リレーI/Fの抽象化で将来の乗り換え余地を残す**折衷とした（§4.2 判断1、Decision Log #3）。

**案C: 分散トランザクション（2PC）でDBとKafkaを束ねる**
利点: 概念上は原子性が直接手に入る。
欠点: KafkaはXAに参加できず、実現には独自コーディネータが必要。可用性・性能・複雑性のすべてで劣後。
不採用理由: 実質的な選択肢にならない。記録のため記載。

**案D: イベントソーシングへの全面移行（outboxではなくイベントストアを正にする）**
利点: 本問題の最も根本的な解決。
欠点: 約定エンジンのアーキテクチャ全面改修であり、規模が2桁違う。
不採用理由: 障害再発防止という時間軸に合わない。将来の検討自体は妨げないが、本docの範囲では扱わない。

## 7. セキュリティ・コンプライアンス考慮

- outbox の payload に要保護情報（約定明細）が平文格納される。アクセス制御は約定テーブルと同一ロールを適用し、リレーの実行ロールは SELECT + published_at UPDATE のみに限定
- リレーの発行ログには event_id とメタデータのみ記録（先行docのログ方針踏襲、payload は記録しない）
- 障害再発防止策としてリスク管理部指摘（R-2026-08）への対応に該当するため、**本docのApprovedをもって指摘対応の方式確定の証跡とする**（リスク管理部・伊藤氏を Reviewer に含めた理由）
- 保持期間7日の妥当性: Decision Log #4 参照

## 8. 運用・移行

3段階で移行する。各段階でロールバック可能。

1. **並走**: outbox書き込み＋リレー発行を有効化しつつ、旧経路（直接発行）も継続。下流は event_id 冪等で重複を吸収するため二重発行は無害。両経路の発行件数を突合し、一致を1週間【仮置き】確認
2. **切替**: 旧経路をフラグで停止。リレーのみ
3. **確定**: 旧経路コードの削除（別PR）。outboxテーブルDROPはしない（本方式の恒久構成要素のため）

リレーは単一プロセス＋自動再起動で開始（停止しても遅延に化けるだけで欠損しないのが本方式の利点。可用性を初期から作り込まない）。監視は「未発行行の滞留数」と「最古未発行行の経過時間」の2メトリクスを新設。

## 9. Open Questions

1. ~~ポーリング間隔100msのDB負荷影響~~ → **解決**: DBチーム試算で問題なし。部分インデックスにより未発行行走査はO(発行待ち件数)（Decision Log #2）
2. ~~リレーの二重起動防止（自動再起動との競合）~~ → **解決**: DBアドバイザリロックで排他。二重起動しても重複発行になるだけで欠損はしないため、ロックは性能保護目的と整理（Decision Log #5）
3. 並走期間の突合で不一致が出た場合の判定手順（どちらを正とするか）→ **未解決のまま実装着手**: 並走開始までに運用チームと手順書化。設計には影響しない
4. 日次突合バッチの縮退時期 → **未解決**: Non-Goals通り本docでは扱わない。四半期後に別途

## 10. レビュー決定記録（Decision Log）

| # | 指摘の要旨 | 提起者 | 決着 | 反映先 |
|---|---|---|---|---|
| 1 | outboxを約定エンジンDBに置くなら、そのDDL変更管理はどちらのチームプロセスに乗るのか | 高橋 | テーブルオーナーは通知基盤チーム、ただしDDL変更時は両チームレビュー必須。命名プレフィクス `notif_` で管轄を明示 | §4.2 判断2 |
| 2 | 100msポーリングの負荷根拠が仮置き | 鈴木 | DBチームで試算、部分インデックス前提で定常負荷は無視できる水準と確認。試算資料はスレッド添付 | §9-1 |
| 3 | **CDCを見送る判断への異議**。「どうせ将来CDCにするなら最初からやるべき。二度手間では」 | 佐藤 | コメント3往復で収束せず**30分の対面レビューにエスカレーション**（プロセス運用ルール§4適用）。対面での整理: CDC移行が確定した将来ではなく可能性である以上、現時点の運用成熟度で確実に回せる方式を先に入れ、リレーI/F抽象化で乗換コストを限定する方針を維持。佐藤の懸念のうち「抽象化が絵に描いた餅にならないか」は、リレーI/Fの具体定義を実装PRの必須レビュー観点とすることで対応。**結論: 設計変更なし、実装レビュー観点に追加** | §4.2 判断1に「異論歓迎→議論済み」注記、議事はスレッド #58 |
| 4 | outbox保持期間7日は保全要件（7年）と矛盾しないか | 伊藤 | 監査保全の対象は「配信の事実の記録」であり、それはWORMログとKafka（保持期間内）＋日次突合結果が担う。outboxは配送待ち行列であり保全対象外と整理。リスク管理部として異議なし | §5.1 コメント、§7 |
| 5 | リレー二重起動時の挙動が未定義 | 田中 | アドバイザリロックで排他。万一破れても重複発行止まり（欠損はしない）ことを設計の前提として明記 | §9-2 |
